{
  "person": false,
  "place": true,
  "expression": "contact && user && user.roles && ( ((contact.type === 'clinic' || contact.contact_type === 'clinic') && user.roles.includes('nurse')) || ((contact.type === 'health_center' || contact.contact_type === 'health_center') && user.roles.includes('nurse_supervisor')) )"
}
