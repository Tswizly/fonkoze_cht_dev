{
  "person": false,
  "place": true,
  "expression": "(user && user.role === 'nurse' && contact && contact.type === 'clinic') || (user && user.role === 'nurse_supervisor' && contact && contact.type === 'health_center')"
}
